# Root
- hosts: opensuse
  remote_user: "{{ ansible_user }}"
  become: true
  roles:
    - zypper
    - user
    - ranger
    - zsh
    - zplug
    - firefox
    - nvchad
    - wireless_driver
    - dnsmasq

# User
- hosts: opensuse
  remote_user: "{{ ansible_user }}"
  become: false
  roles:
    - flatpak
    - ranger
    - zsh
    - zplug
    - nvchad
    - autostart
    # - nerd_fonts
