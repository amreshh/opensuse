---
  desktop:
    hosts:
      opensuse:
        ansible_host: 192.168.100.239
        ansible_user: "{{ opensuse_user }}"
        ansible_password: "{{ opensuse_password }}"
        ansible_sudo_pass: "{{ opensuse_sudo_pass }}"
